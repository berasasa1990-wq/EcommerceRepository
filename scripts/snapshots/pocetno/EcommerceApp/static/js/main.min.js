function stabilizeMobileProductCards(){if(window.innerWidth>768)return;document.querySelectorAll('.product-card').forEach((card)=>{card.style.transform='none';});document.querySelectorAll('.product-image img[data-main-image]').forEach((img)=>{img.style.opacity='1';});}
window.addEventListener('pageshow',(event)=>{if(event.persisted){stabilizeMobileProductCards();}});document.addEventListener('DOMContentLoaded',()=>{const scrollTopBtn=document.getElementById('scrollTopBtn');if(scrollTopBtn){const toggleScrollTopBtn=()=>{scrollTopBtn.classList.toggle('is-visible',window.scrollY>320);};toggleScrollTopBtn();window.addEventListener('scroll',toggleScrollTopBtn,{passive:true});scrollTopBtn.addEventListener('click',()=>{window.scrollTo({top:0,behavior:'smooth'});});}
document.querySelectorAll('[data-seo-collapse]').forEach((block)=>{const toggle=block.querySelector('[data-seo-toggle]');const preview=block.querySelector('[data-seo-preview]');const full=block.querySelector('[data-seo-full]');if(!toggle||!preview||!full)return;toggle.addEventListener('click',()=>{const expanded=block.classList.toggle('is-expanded');block.classList.toggle('is-collapsed',!expanded);if(expanded){full.hidden=false;preview.hidden=true;toggle.setAttribute('aria-expanded','true');toggle.textContent='Prikaži manje';}else{full.hidden=true;preview.hidden=false;toggle.setAttribute('aria-expanded','false');toggle.textContent='Prikaži više';}});});const header=document.getElementById('header');const navToggle=document.getElementById('navToggle');const navLinks=document.getElementById('navLinks');const headerSearch=document.getElementById('headerSearch');const searchClose=document.getElementById('searchClose');if(typeof window.syncSiteChromeOffset==='function'){window.syncSiteChromeOffset();window.addEventListener('resize',()=>window.syncSiteChromeOffset(),{passive:true});}
window.addEventListener('scroll',()=>{header.classList.toggle('scrolled',window.scrollY>10);});const megaMenuPanel=document.getElementById('megaMenuPanel');const megaItems=document.querySelectorAll('[data-nav-item].has-mega-menu');const megaMenus=document.querySelectorAll('[data-mega-menu]');let megaCloseTimer;function syncMegaPanelHeight(){if(!megaMenuPanel)return;megaMenuPanel.style.removeProperty('min-height');megaMenuPanel.classList.remove('is-height-synced');}
function positionMegaMenu(item,menu){if(!megaMenuPanel||!header||window.innerWidth<=1024)return;const link=item.querySelector(':scope > a');if(!link)return;const headerRect=header.getBoundingClientRect();const linkRect=link.getBoundingClientRect();const left=Math.max(12,Math.round(linkRect.left-headerRect.left));megaMenuPanel.style.left=`${left}px`;megaMenuPanel.style.right='auto';megaMenuPanel.style.width='auto';megaMenuPanel.style.minWidth=`${Math.max(Math.round(linkRect.width), 220)}px`;}
function closeMegaMenu(){header?.classList.remove('mega-active');megaItems.forEach((el)=>el.classList.remove('mega-open'));megaMenus.forEach((menu)=>menu.classList.remove('active'));syncMobileNavExpanded();}
function syncMobileNavExpanded(){if(!navLinks)return;const hasOpen=navLinks.querySelector(':scope > .nav-item.mega-open');navLinks.classList.toggle('mobile-nav-has-open',Boolean(hasOpen));}
const mobileNavPanel=document.getElementById('mobileNavPanel');const mobileNavClose=document.getElementById('mobileNavClose');const mobileNavSubview=document.getElementById('mobileNavSubview');const mobileNavSubTitle=document.getElementById('mobileNavSubTitle');const mobileNavSubBody=document.getElementById('mobileNavSubBody');const mobileNavBack=document.getElementById('mobileNavBack');let mobileNavScrollLockY=0;function syncMobileNavLayout(){if(window.innerWidth>1024)return;const primary=header?.querySelector('.header-row--primary');const promo=document.querySelector('.promo-bar');let top=0;if(primary){top=Math.round(primary.getBoundingClientRect().bottom);}else if(header){top=Math.round(header.getBoundingClientRect().bottom);}else if(promo){top=Math.round(promo.getBoundingClientRect().bottom);}
top=Math.max(top,48);document.documentElement.style.setProperty('--mobile-nav-panel-top',`${top}px`);if(typeof window.syncSiteChromeOffset==='function'){window.syncSiteChromeOffset();}}
function resetMobileNavScroll(){if(navLinks)navLinks.scrollTop=0;if(mobileNavPanel)mobileNavPanel.scrollTop=0;if(mobileNavSubview)mobileNavSubview.scrollTop=0;}
let mobileNavSubviewCloseTimer=null;function closeMobileNavSubview(immediate){if(!mobileNavSubview)return;if(mobileNavSubviewCloseTimer){clearTimeout(mobileNavSubviewCloseTimer);mobileNavSubviewCloseTimer=null;}
const wasOpen=mobileNavSubview.classList.contains('is-open')||mobileNavSubview.classList.contains('is-closing');const finish=()=>{mobileNavSubview.classList.remove('is-open','is-closing');mobileNavSubview.setAttribute('aria-hidden','true');navLinks?.classList.remove('mobile-nav-sub-open');megaItems.forEach((el)=>el.classList.remove('mega-open'));try{if(document.activeElement&&document.activeElement.blur){document.activeElement.blur();}}catch(e){}
navLinks?.querySelectorAll('a').forEach((a)=>{try{a.blur();}catch(e){}});syncMobileNavExpanded();mobileNavSubviewCloseTimer=null;};if(!wasOpen||immediate||window.matchMedia('(prefers-reduced-motion: reduce)').matches){finish();return;}
mobileNavSubview.classList.remove('is-open');mobileNavSubview.classList.add('is-closing');mobileNavSubview.setAttribute('aria-hidden','true');mobileNavSubviewCloseTimer=setTimeout(finish,260);}
function buildMobileNavSubLink(anchor,className){const li=document.createElement('li');const link=document.createElement('a');link.href=anchor.getAttribute('href')||'#';link.textContent=anchor.textContent.trim();if(className)link.className=className;link.addEventListener('click',()=>setMobileNavOpen(false));li.appendChild(link);return li;}
function openMobileNavSubview(item){const link=item.querySelector(':scope > a');const submenu=item.querySelector('.nav-submenu');if(!link||!submenu||!mobileNavSubview||!mobileNavSubBody||!mobileNavSubTitle)return;mobileNavSubTitle.textContent=link.childNodes[0]?.textContent?.trim()||link.textContent.trim();mobileNavSubBody.innerHTML='';const list=document.createElement('ul');list.className='mobile-nav-subview-list';submenu.querySelectorAll(':scope > li').forEach((entry)=>{if(entry.classList.contains('nav-submenu-all')){const anchor=entry.querySelector('a');if(anchor)list.appendChild(buildMobileNavSubLink(anchor,'mobile-nav-subview-link mobile-nav-subview-link--all'));return;}
const titleLink=entry.querySelector(':scope > a.nav-submenu-title, :scope > a');if(titleLink){list.appendChild(buildMobileNavSubLink(titleLink,'mobile-nav-subview-link mobile-nav-subview-link--title'));}
entry.querySelectorAll('.nav-submenu-nested a').forEach((nestedLink)=>{list.appendChild(buildMobileNavSubLink(nestedLink,'mobile-nav-subview-link mobile-nav-subview-link--child'));});});mobileNavSubBody.appendChild(list);megaItems.forEach((el)=>el.classList.remove('mega-open'));item.classList.add('mega-open');navLinks?.classList.add('mobile-nav-sub-open');if(mobileNavSubviewCloseTimer){clearTimeout(mobileNavSubviewCloseTimer);mobileNavSubviewCloseTimer=null;}
mobileNavSubview.classList.remove('is-open','is-closing');void mobileNavSubview.offsetWidth;mobileNavSubview.classList.add('is-open');mobileNavSubview.setAttribute('aria-hidden','false');mobileNavSubview.scrollTop=0;syncMobileNavExpanded();}
function setMobileNavOpen(isOpen){const open=Boolean(isOpen);navLinks?.classList.toggle('mobile-open',open);mobileNavPanel?.classList.toggle('is-open',open);document.documentElement.classList.toggle('mobile-nav-open',open);document.body.classList.toggle('mobile-nav-open',open);if(navToggle){navToggle.setAttribute('aria-expanded',open?'true':'false');navToggle.setAttribute('aria-label',open?'Zatvori meni':'Otvori meni');}
if(open){mobileNavScrollLockY=window.scrollY||window.pageYOffset||0;document.body.style.width='100%';closeSearchOverlay();closeMobileNavSubview(true);syncMobileNavLayout();requestAnimationFrame(()=>{syncMobileNavLayout();resetMobileNavScroll();});setTimeout(()=>{syncMobileNavLayout();resetMobileNavScroll();},50);}else{document.body.style.width='';document.documentElement.style.removeProperty('--mobile-nav-panel-top');resetMobileNavScroll();closeMegaMenu();closeMobileNavSubview(true);if(Math.abs((window.scrollY||0)-mobileNavScrollLockY)>2){window.scrollTo(0,mobileNavScrollLockY);}}
syncMobileNavExpanded();}
function onNavToggleActivate(e){e.preventDefault();e.stopPropagation();const isOpen=navLinks?.classList.contains('mobile-open');setMobileNavOpen(!isOpen);}
if(navToggle){navToggle.addEventListener('click',onNavToggleActivate);navToggle.setAttribute('aria-expanded','false');navToggle.setAttribute('aria-controls','mobileNavPanel');}
const catsDropdown=document.getElementById('catsDropdown');const catsDropdownBtn=document.getElementById('catsDropdownBtn');const catsDropdownPanel=document.getElementById('catsDropdownPanel');const catsLockedOpen=Boolean(catsDropdown?.classList.contains('cats-dropdown--locked')||document.body.classList.contains('cats-sidebar-open'));function setCatsDropdownOpen(open){if(!catsDropdown||!catsDropdownBtn)return;if(catsLockedOpen&&!open)return;catsDropdown.classList.toggle('is-open',open);catsDropdownBtn.setAttribute('aria-expanded',open?'true':'false');if(catsDropdownPanel)catsDropdownPanel.hidden=!open;document.body.classList.toggle('cats-sidebar-open',open&&window.innerWidth>1024);}
if(catsDropdown?.classList.contains('is-open')||catsLockedOpen){setCatsDropdownOpen(true);}
if(catsDropdownBtn&&catsDropdown){catsDropdownBtn.addEventListener('click',(e)=>{e.preventDefault();e.stopPropagation();if(catsLockedOpen)return;setCatsDropdownOpen(!catsDropdown.classList.contains('is-open'));});catsDropdown.addEventListener('click',(e)=>{const toggle=e.target.closest('[data-cats-toggle]');if(!toggle||!catsDropdown.contains(toggle))return;e.preventDefault();e.stopPropagation();const item=toggle.closest('.cats-dropdown__item--has-sub');if(!item)return;const willOpen=!item.classList.contains('is-open');catsDropdown.querySelectorAll('.cats-dropdown__item--has-sub.is-open').forEach((el)=>{if(el!==item){el.classList.remove('is-open');const btn=el.querySelector('[data-cats-toggle]');if(btn)btn.setAttribute('aria-expanded','false');}});item.classList.toggle('is-open',willOpen);toggle.setAttribute('aria-expanded',willOpen?'true':'false');});document.addEventListener('click',(e)=>{if(catsLockedOpen)return;if(!catsDropdown.contains(e.target))setCatsDropdownOpen(false);});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&!catsLockedOpen)setCatsDropdownOpen(false);});}
mobileNavClose?.addEventListener('click',(e)=>{e.preventDefault();setMobileNavOpen(false);});function openMegaMenu(item){clearTimeout(megaCloseTimer);const targetId=item.dataset.megaTarget;const menu=document.getElementById(targetId);if(!menu)return;megaItems.forEach((el)=>el.classList.remove('mega-open'));megaMenus.forEach((m)=>m.classList.remove('active'));item.classList.add('mega-open');menu.classList.add('active');header?.classList.add('mega-active');positionMegaMenu(item,menu);}
function scheduleMegaClose(){clearTimeout(megaCloseTimer);megaCloseTimer=setTimeout(closeMegaMenu,180);}
function forceCloseMegaMenu(){clearTimeout(megaCloseTimer);closeMegaMenu();}
if(window.innerWidth>1024){syncMegaPanelHeight();megaItems.forEach((item)=>{item.addEventListener('mouseenter',()=>openMegaMenu(item));});document.querySelectorAll('[data-nav-item]:not(.has-mega-menu)').forEach((item)=>{item.addEventListener('mouseenter',()=>{forceCloseMegaMenu();});});const categoriesRow=header?.querySelector('.header-row--categories');const megaKeepOpenZones=[categoriesRow,megaMenuPanel].filter(Boolean);megaKeepOpenZones.forEach((zone)=>{zone.addEventListener('mouseenter',()=>clearTimeout(megaCloseTimer));zone.addEventListener('mouseleave',scheduleMegaClose);});const megaCloseOnHover=[headerSearch,header?.querySelector('.logo-brand'),header?.querySelector('.nav-actions'),header?.querySelector('.header-row--primary'),].filter(Boolean);megaCloseOnHover.forEach((el)=>{el.addEventListener('mouseenter',forceCloseMegaMenu);});}
window.addEventListener('load',syncMegaPanelHeight);window.addEventListener('resize',()=>{syncMegaPanelHeight();if(window.innerWidth>1024){const activeItem=document.querySelector('[data-nav-item].mega-open');const activeMenu=document.querySelector('[data-mega-menu].active');if(activeItem&&activeMenu){positionMegaMenu(activeItem,activeMenu);}}});megaItems.forEach((item)=>{const link=item.querySelector(':scope > a');const submenu=item.querySelector('.nav-submenu');link?.addEventListener('click',(e)=>{const menuOpen=navLinks?.classList.contains('mobile-open')||mobileNavPanel?.classList.contains('is-open');if(window.innerWidth<=1024&&menuOpen&&submenu){e.preventDefault();e.stopPropagation();if(item.classList.contains('mega-open')&&mobileNavSubview?.classList.contains('is-open')){closeMobileNavSubview();return;}
openMobileNavSubview(item);requestAnimationFrame(syncMobileNavLayout);}});});mobileNavBack?.addEventListener('click',(e)=>{e.preventDefault();closeMobileNavSubview();});window.addEventListener('resize',()=>{if(window.innerWidth>1024){setMobileNavOpen(false);return;}
if(navLinks?.classList.contains('mobile-open')){syncMobileNavLayout();}});const searchForm=document.getElementById('searchForm');const searchInput=document.getElementById('searchInput');const searchCombobox=document.querySelector('.search-combobox');const searchSuggestions=document.getElementById('searchSuggestions');const searchSuggestUrl='/api/pretraga/';let searchDebounceTimer;let searchFetchController;const placeholderThumbSvg=`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>`;function focusSearchInput(){if(!searchInput)return;searchInput.focus({preventScroll:true});const end=searchInput.value.length;if(typeof searchInput.setSelectionRange==='function'){searchInput.setSelectionRange(end,end);}}
function scheduleSearchFocus(){focusSearchInput();requestAnimationFrame(()=>{requestAnimationFrame(focusSearchInput);});window.setTimeout(focusSearchInput,50);window.setTimeout(focusSearchInput,320);}
function setSuggestionsExpanded(expanded){const target=searchCombobox||searchInput;if(!target)return;target.setAttribute('aria-expanded',expanded?'true':'false');}
function setSearchSuggestActive(active){document.body.classList.toggle('search-suggest-open',Boolean(active));}
function clearSearchSuggestions(){if(!searchSuggestions)return;searchSuggestions.innerHTML='';searchSuggestions.hidden=true;searchSuggestions.style.position='';searchSuggestions.style.left='';searchSuggestions.style.top='';searchSuggestions.style.width='';searchSuggestions.style.right='';setSuggestionsExpanded(false);setSearchSuggestActive(false);}
function positionSearchSuggestions(){if(!searchSuggestions||searchSuggestions.hidden||!headerSearch)return;const rect=headerSearch.getBoundingClientRect();searchSuggestions.style.position='fixed';searchSuggestions.style.left=Math.round(rect.left)+'px';searchSuggestions.style.width=Math.round(rect.width)+'px';searchSuggestions.style.right='auto';searchSuggestions.style.top=Math.round(rect.bottom+4)+'px';searchSuggestions.style.zIndex='14000';}
function searchSuggestLimit(){return window.innerWidth<=768?4:8;}
function renderSearchSuggestions(results,query,hasMore=false){if(!searchSuggestions)return;if(!query){clearSearchSuggestions();return;}
if(!results.length){searchSuggestions.innerHTML=`<div role="status" class="search-suggestions-empty">Nema artikala za „${escapeHtml(query)}".</div>`;searchSuggestions.hidden=false;setSuggestionsExpanded(true);setSearchSuggestActive(true);positionSearchSuggestions();return;}
const mobile=window.innerWidth<=768;const shown=mobile?results.slice(0,4):results;const items=shown.map((item)=>{const thumb=item.image?`<img src="${escapeHtml(item.image)}" alt="" width="48" height="48" loading="lazy" decoding="async">`:placeholderThumbSvg;const priceClass=item.on_sale?' search-suggestion-price--sale':'';const pack=item.pack?`<span class="search-suggestion-pack">${escapeHtml(item.pack)}</span>`:'';return`<a href="${escapeHtml(item.url)}" class="search-suggestion" role="option">
                <span class="search-suggestion-thumb">${thumb}</span>
                <span class="search-suggestion-copy">
                    <span class="search-suggestion-name">${escapeHtml(item.naziv)}</span>
                    ${pack}
                </span>
                <span class="search-suggestion-price${priceClass}">${escapeHtml(item.price)} KM</span>
            </a>`;}).join('');const base=searchForm?.getAttribute('action')||'/';const allResultsUrl=`${base}?q=${encodeURIComponent(query)}#product-showcase`;const showFooter=mobile?shown.length>0:hasMore;const footerLabel=mobile?'Otvori sve':`Vidi sve rezultate za „${escapeHtml(query)}"`;const footer=showFooter?`<p class="search-suggestions-footer"><a href="${escapeHtml(allResultsUrl)}">${footerLabel}</a></p>`:'';searchSuggestions.innerHTML=items+footer;searchSuggestions.hidden=false;setSuggestionsExpanded(true);setSearchSuggestActive(true);positionSearchSuggestions();}
function escapeHtml(value){return String(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
async function fetchSearchSuggestions(query){if(!searchSuggestions)return;searchFetchController?.abort();searchFetchController=new AbortController();const loadingTimer=window.setTimeout(()=>{if(searchInput?.value.trim()!==query)return;searchSuggestions.innerHTML='<div role="status" class="search-suggestions-loading">Pretraga…</div>';searchSuggestions.hidden=false;setSuggestionsExpanded(true);setSearchSuggestActive(true);positionSearchSuggestions();},140);try{const response=await fetch(`${searchSuggestUrl}?q=${encodeURIComponent(query)}&limit=${searchSuggestLimit()}`,{headers:{'X-Requested-With':'XMLHttpRequest','Accept':'application/json'},signal:searchFetchController.signal,});if(!response.ok)throw new Error('Search failed');const data=await response.json();if(searchInput?.value.trim()!==query)return;window.clearTimeout(loadingTimer);renderSearchSuggestions(data.results||[],data.query||query,Boolean(data.has_more));}catch(error){window.clearTimeout(loadingTimer);if(error.name==='AbortError')return;searchSuggestions.innerHTML='<div role="status" class="search-suggestions-empty">Pretraga trenutno nije dostupna.</div>';searchSuggestions.hidden=false;setSearchSuggestActive(true);positionSearchSuggestions();}}
function queueSearchSuggestions(){clearTimeout(searchDebounceTimer);const value=searchInput?.value.trim()||'';if(!value){clearSearchSuggestions();return;}
const delay=value.length<=3?280:200;searchDebounceTimer=window.setTimeout(()=>fetchSearchSuggestions(value),delay);}
function openSearchOverlay(){forceCloseMegaMenu();if(window.innerWidth>1024){focusSearchInput();return;}
header?.classList.add('header-search-open');setMobileNavOpen(false);scheduleSearchFocus();if(searchInput?.value.trim()){queueSearchSuggestions();}}
function closeSearchOverlay(){header?.classList.remove('header-search-open');clearSearchSuggestions();searchFetchController?.abort();}
function navigateToSearch(query){const trimmed=query.trim();if(!trimmed||!searchForm)return;const base=searchForm.getAttribute('action')||'/';const target=`${base}?q=${encodeURIComponent(trimmed)}#product-showcase`;if(`${window.location.pathname}${window.location.search}${window.location.hash}`===target){closeSearchOverlay();const showcase=document.getElementById('product-showcase');showcase?.scrollIntoView({block:'start'});return;}
window.location.assign(target);}
searchClose?.addEventListener('click',closeSearchOverlay);searchForm?.addEventListener('submit',(e)=>{e.preventDefault();navigateToSearch(searchInput?.value||'');});searchInput?.addEventListener('focus',forceCloseMegaMenu);searchInput?.addEventListener('pointerdown',forceCloseMegaMenu);headerSearch?.addEventListener('mouseenter',forceCloseMegaMenu);searchInput?.addEventListener('input',queueSearchSuggestions);searchInput?.addEventListener('keyup',queueSearchSuggestions);searchInput?.addEventListener('search',queueSearchSuggestions);window.addEventListener('resize',positionSearchSuggestions,{passive:true});window.addEventListener('scroll',positionSearchSuggestions,{passive:true});const urlParams=new URLSearchParams(window.location.search);const activeSearchQuery=urlParams.get('q')?.trim();const CATALOG_SCROLL_KEY='catalogFilterScrollY';const saveCatalogScroll=()=>{sessionStorage.setItem(CATALOG_SCROLL_KEY,String(window.scrollY));};const restoreCatalogScroll=()=>{const saved=sessionStorage.getItem(CATALOG_SCROLL_KEY);if(saved===null)return false;sessionStorage.removeItem(CATALOG_SCROLL_KEY);const y=Number.parseInt(saved,10);if(!Number.isNaN(y)){window.scrollTo(0,y);return true;}
return false;};const scrollToProductShowcase=()=>{const showcase=document.getElementById('product-showcase');if(showcase){requestAnimationFrame(()=>{showcase.scrollIntoView({block:'start',behavior:'smooth'});});}};const isCatalogFilterRefinement=urlParams.has('velicina')||urlParams.has('cijena_od')||urlParams.has('cijena_do')||urlParams.has('sort')||urlParams.has('page');if(activeSearchQuery){document.body.classList.add('search-results-active');}
const restoredCatalogScroll=restoreCatalogScroll();if(!restoredCatalogScroll&&!isCatalogFilterRefinement){if(activeSearchQuery||urlParams.has('akcija')||urlParams.has('noviteti')||urlParams.has('brend')||urlParams.has('kategorija')||window.location.hash==='#product-showcase'){scrollToProductShowcase();}}
const catalogFilterForm=document.querySelector('.catalog-filter');const catalogSidebar=document.querySelector('.catalog-sidebar');const syncFilterExpanded=()=>{const openGroup=catalogFilterForm?.querySelector('.filter-group--collapsible.is-open');const isOpen=!!openGroup;catalogFilterForm?.querySelectorAll('.filter-below-hidden').forEach((el)=>{el.classList.remove('filter-below-hidden');});catalogFilterForm?.classList.toggle('is-filter-expanded',isOpen);catalogSidebar?.classList.toggle('is-filter-expanded',isOpen);if(!openGroup||!catalogFilterForm)return;let hideBelow=false;Array.from(catalogFilterForm.children).forEach((child)=>{if(child===openGroup){hideBelow=true;return;}
if(hideBelow){child.classList.add('filter-below-hidden');}});};const closeFilterSizeGroup=(group,{skipSync=false}={})=>{if(!group)return;group.classList.remove('is-open');const toggle=group.querySelector('.filter-size-toggle');toggle?.setAttribute('aria-expanded','false');if(!skipSync)syncFilterExpanded();};const openFilterSizeGroup=(group)=>{if(!group)return;document.querySelectorAll('.filter-group--collapsible').forEach((other)=>{if(other!==group)closeFilterSizeGroup(other,{skipSync:true});});group.classList.add('is-open');const toggle=group.querySelector('.filter-size-toggle');toggle?.setAttribute('aria-expanded','true');syncFilterExpanded();};syncFilterExpanded();document.querySelectorAll('.filter-size-toggle').forEach((toggle)=>{toggle.addEventListener('click',()=>{const group=toggle.closest('.filter-group--collapsible');if(!group)return;if(group.classList.contains('is-open')){closeFilterSizeGroup(group);}else{openFilterSizeGroup(group);}});});document.querySelectorAll('.filter-size-option, .filter-size-clear').forEach((link)=>{link.addEventListener('click',saveCatalogScroll);});catalogFilterForm?.addEventListener('submit',saveCatalogScroll);catalogFilterForm?.querySelector('.btn-filter.btn-secondary')?.addEventListener('click',saveCatalogScroll);document.querySelectorAll('.catalog-desktop-filter').forEach((form)=>{form.addEventListener('submit',saveCatalogScroll);form.querySelector('.catalog-desktop-filter__reset')?.addEventListener('click',saveCatalogScroll);});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){closeSearchOverlay();closeMegaMenu();if(mobileNavSubview?.classList.contains('is-open')){closeMobileNavSubview();}else{setMobileNavOpen(false);}}});const carousel=document.getElementById('heroCarousel');if(carousel){const slides=carousel.querySelectorAll('.carousel-slide');const dots=carousel.querySelectorAll('.dot');const prevBtn=document.getElementById('carouselPrev');const nextBtn=document.getElementById('carouselNext');let currentIndex=0;let autoplayTimer;function syncSlideVideos(){slides.forEach((slide,i)=>{const video=slide.querySelector('video');if(!video)return;if(i===currentIndex){video.currentTime=0;video.play().catch(()=>{});}else{video.pause();}});}
function goToSlide(index){const total=slides.length;currentIndex=((index%total)+total)%total;slides.forEach((slide,i)=>slide.classList.toggle('active',i===currentIndex));dots.forEach((dot,i)=>dot.classList.toggle('active',i===currentIndex));syncSlideVideos();}
function startAutoplay(){autoplayTimer=setInterval(()=>goToSlide(currentIndex+1),5000);}
function resetAutoplay(){clearInterval(autoplayTimer);startAutoplay();}
prevBtn?.addEventListener('click',()=>{goToSlide(currentIndex-1);resetAutoplay();});nextBtn?.addEventListener('click',()=>{goToSlide(currentIndex+1);resetAutoplay();});dots.forEach((dot)=>{dot.addEventListener('click',()=>{goToSlide(parseInt(dot.dataset.index,10));resetAutoplay();});});let touchStartX=0;carousel.addEventListener('touchstart',(e)=>{touchStartX=e.touches[0].clientX;},{passive:true});carousel.addEventListener('touchend',(e)=>{const diff=touchStartX-e.changedTouches[0].clientX;if(Math.abs(diff)>50){goToSlide(diff>0?currentIndex+1:currentIndex-1);resetAutoplay();}},{passive:true});syncSlideVideos();startAutoplay();}
function initHomeProductCarousels(){const carousels=document.querySelectorAll('.home-product-carousel');if(!carousels.length)return;carousels.forEach((root)=>{const viewport=root.querySelector('.home-product-carousel__viewport');const track=root.querySelector('.home-product-carousel__track');const slides=root.querySelectorAll('.home-product-carousel__slide');const prevBtn=root.querySelector('.home-product-carousel__btn--prev');const nextBtn=root.querySelector('.home-product-carousel__btn--next');if(!viewport||!track||!slides.length)return;let index=0;let autoplayTimer=null;let gap=8;const autoplayMs=parseInt(root.dataset.autoplayMs,10)||4500;function visibleCount(){const mobile=parseInt(root.dataset.visibleMobile,10)||2;const desktop=parseInt(root.dataset.visibleDesktop,10)||5;return window.innerWidth<=768?mobile:desktop;}
function maxIndex(){return Math.max(0,slides.length-visibleCount());}
function slideStep(){const slideWidth=parseFloat(getComputedStyle(root).getPropertyValue('--carousel-slide-width'));return slideWidth+gap;}
function readGap(){const trackStyles=getComputedStyle(track);const parsed=parseFloat(trackStyles.columnGap||trackStyles.gap);return Number.isFinite(parsed)?parsed:8;}
function updateLayout(){gap=readGap();const visible=visibleCount();const viewportWidth=viewport.clientWidth;const slideWidth=(viewportWidth-(visible-1)*gap)/visible;root.style.setProperty('--carousel-slide-width',`${slideWidth}px`);index=Math.min(index,maxIndex());updatePosition();}
function updatePosition(){const offset=index*slideStep();track.style.transform=`translate3d(-${offset}px, 0, 0)`;const isStatic=slides.length<=visibleCount();if(prevBtn)prevBtn.disabled=isStatic;if(nextBtn)nextBtn.disabled=isStatic;root.classList.toggle('home-product-carousel--static',isStatic);}
function goTo(nextIndex,{animate=true}={}){const limit=maxIndex();if(limit===0){index=0;updatePosition();return;}
if(nextIndex>limit){track.style.transition=animate?'':'none';index=0;updatePosition();if(!animate){requestAnimationFrame(()=>{void track.offsetWidth;track.style.transition='';});}
return;}
if(nextIndex<0){track.style.transition=animate?'':'none';index=limit;updatePosition();if(!animate){requestAnimationFrame(()=>{void track.offsetWidth;track.style.transition='';});}
return;}
track.style.transition='';index=nextIndex;updatePosition();}
function startAutoplay(){clearInterval(autoplayTimer);if(slides.length<=visibleCount())return;autoplayTimer=setInterval(()=>{if(index>=maxIndex()){goTo(0,{animate:false});}else{goTo(index+1);}},autoplayMs);}
function resetAutoplay(){clearInterval(autoplayTimer);startAutoplay();}
prevBtn?.addEventListener('click',()=>{if(index<=0){goTo(maxIndex(),{animate:false});}else{goTo(index-1);}
resetAutoplay();});nextBtn?.addEventListener('click',()=>{if(index>=maxIndex()){goTo(0,{animate:false});}else{goTo(index+1);}
resetAutoplay();});root.addEventListener('mouseenter',()=>clearInterval(autoplayTimer));root.addEventListener('mouseleave',startAutoplay);root.addEventListener('focusin',()=>clearInterval(autoplayTimer));root.addEventListener('focusout',startAutoplay);let touchStartX=0;let touchStartY=0;let touchActive=false;const SWIPE_MIN=36;function suppressNextClick(){const onClick=(ev)=>{ev.preventDefault();ev.stopPropagation();viewport.removeEventListener('click',onClick,true);};viewport.addEventListener('click',onClick,true);window.setTimeout(()=>{viewport.removeEventListener('click',onClick,true);},450);}
function stepFromDelta(dx){if(dx>0){if(index>=maxIndex())goTo(0,{animate:false});else goTo(index+1);}else{if(index<=0)goTo(maxIndex(),{animate:false});else goTo(index-1);}
resetAutoplay();}
viewport.addEventListener('touchstart',(e)=>{if(e.touches.length!==1)return;if(e.target&&e.target.closest('button, [data-catalog-add], .catalog-variation-modal')){touchActive=false;return;}
const t=e.touches[0];touchStartX=t.clientX;touchStartY=t.clientY;touchActive=true;},{passive:true});viewport.addEventListener('touchend',(e)=>{if(!touchActive)return;touchActive=false;const t=e.changedTouches[0];if(!t)return;const dx=touchStartX-t.clientX;const dy=touchStartY-t.clientY;if(Math.abs(dx)<SWIPE_MIN||Math.abs(dx)<Math.abs(dy))return;stepFromDelta(dx);suppressNextClick();},{passive:true});viewport.addEventListener('touchcancel',()=>{touchActive=false;},{passive:true});let pointerTracking=false;let pointerDragging=false;let pointerId=null;let pointerStartX=0;let pointerStartY=0;viewport.addEventListener('pointerdown',(e)=>{if(e.pointerType==='touch')return;if(e.button!==0)return;if(e.target&&e.target.closest('button, [data-catalog-add], .catalog-variation-modal')){return;}
pointerTracking=true;pointerDragging=false;pointerId=e.pointerId;pointerStartX=e.clientX;pointerStartY=e.clientY;});viewport.addEventListener('pointermove',(e)=>{if(!pointerTracking||e.pointerId!==pointerId)return;const dx=e.clientX-pointerStartX;const dy=e.clientY-pointerStartY;if(!pointerDragging){if(Math.abs(dx)<SWIPE_MIN||Math.abs(dx)<Math.abs(dy))return;pointerDragging=true;root.classList.add('is-dragging');try{viewport.setPointerCapture(e.pointerId);}catch(_){}}});function endPointerDrag(e){if(!pointerTracking||(pointerId!=null&&e.pointerId!==pointerId))return;const wasDragging=pointerDragging;const startX=pointerStartX;const startY=pointerStartY;pointerTracking=false;pointerDragging=false;pointerId=null;root.classList.remove('is-dragging');try{if(wasDragging)viewport.releasePointerCapture(e.pointerId);}catch(_){}
if(!wasDragging)return;const dx=startX-e.clientX;const dy=startY-e.clientY;if(Math.abs(dx)<SWIPE_MIN||Math.abs(dx)<Math.abs(dy))return;stepFromDelta(dx);suppressNextClick();}
viewport.addEventListener('pointerup',endPointerDrag);viewport.addEventListener('pointercancel',(e)=>{if(pointerId!=null&&e.pointerId!==pointerId)return;pointerTracking=false;pointerDragging=false;pointerId=null;root.classList.remove('is-dragging');});window.addEventListener('resize',updateLayout);updateLayout();startAutoplay();});}
initHomeProductCarousels();initAccountDashboard();initContactFloat();function initAccountDashboard(){const root=document.querySelector('[data-account-dash]');if(!root)return;const panels=Array.from(root.querySelectorAll('[data-acc-panel]'));const links=Array.from(root.querySelectorAll('[data-acc-nav]'));const valid=new Set(panels.map((panel)=>panel.dataset.accPanel));function show(id){const section=valid.has(id)?id:'pregled';panels.forEach((panel)=>{panel.hidden=panel.dataset.accPanel!==section;});links.forEach((link)=>{link.classList.toggle('is-active',link.dataset.accNav===section);});window.dispatchEvent(new Event('resize'));}
function fromHash(){const fromUrl=(window.location.hash||'').replace('#','');const initial=root.dataset.initialSection||'pregled';show(fromUrl||initial);}
window.addEventListener('hashchange',fromHash);fromHash();root.querySelectorAll('[data-copy]').forEach((btn)=>{btn.addEventListener('click',async()=>{const target=document.getElementById(btn.getAttribute('data-copy'));const text=(target&&target.textContent||'').trim();if(!text)return;try{await navigator.clipboard.writeText(text);}catch(err){const range=document.createRange();range.selectNodeContents(target);const sel=window.getSelection();sel.removeAllRanges();sel.addRange(range);document.execCommand('copy');sel.removeAllRanges();}
const original=btn.textContent;btn.textContent='Kopirano';window.setTimeout(()=>{btn.textContent=original;},1600);});});}
function initContactFloat(){const group=document.getElementById('contactFloatGroup');if(!group)return;const toggle=group.querySelector('[data-contact-float-toggle]');const menu=group.querySelector('[data-contact-float-menu]');if(!toggle||!menu)return;function setOpen(open){group.classList.toggle('is-open',open);toggle.setAttribute('aria-expanded',open?'true':'false');toggle.setAttribute('aria-label',open?'Zatvori kontakt opcije':'Kontakt — odaberi aplikaciju',);}
setOpen(false);toggle.addEventListener('click',(event)=>{event.preventDefault();event.stopPropagation();setOpen(!group.classList.contains('is-open'));});document.addEventListener('click',(event)=>{if(!group.classList.contains('is-open'))return;if(!group.contains(event.target)){setOpen(false);}});document.addEventListener('keydown',(event)=>{if(event.key==='Escape'&&group.classList.contains('is-open')){setOpen(false);}});}
function getCsrfToken(){const meta=document.querySelector('meta[name="csrf-token"]');if(meta&&meta.content)return meta.content;const input=document.querySelector('input[name="csrfmiddlewaretoken"]');if(input&&input.value)return input.value;const match=document.cookie.match(/(?:^|;\s*)csrftoken=([^;]+)/);return match?decodeURIComponent(match[1]):'';}
function triggerCartAddPulse(cartBtn){cartBtn.classList.remove('cart-btn--pulse-once');void cartBtn.offsetWidth;cartBtn.classList.add('cart-btn--pulse-once');const svg=cartBtn.querySelector('.cart-btn-svg');if(!svg){cartBtn.classList.remove('cart-btn--pulse-once');return;}
const clearPulse=(event)=>{if(event.animationName!=='cart-icon-add-pulse')return;cartBtn.classList.remove('cart-btn--pulse-once');svg.removeEventListener('animationend',clearPulse);};svg.addEventListener('animationend',clearPulse);}
function updateCartBadge(count){const cartBtn=document.querySelector('.cart-btn');if(!cartBtn)return;const previousCount=parseInt(cartBtn.dataset.cartCount||'0',10);const nextCount=Math.max(0,parseInt(count,10)||0);let badge=cartBtn.querySelector('.cart-badge');if(!badge){badge=document.createElement('span');badge.className='cart-badge';const icon=cartBtn.querySelector('.nav-action-icon');(icon||cartBtn).appendChild(badge);}
badge.hidden=false;badge.style.display='';badge.textContent=String(nextCount);if(nextCount>0){cartBtn.classList.add('cart-btn--has-items');if(nextCount>previousCount){triggerCartAddPulse(cartBtn);}}else{cartBtn.classList.remove('cart-btn--has-items','cart-btn--pulse-once');}
cartBtn.dataset.cartCount=String(nextCount);cartBtn.setAttribute('data-cart-count',String(nextCount));}
window.updateCartBadge=updateCartBadge;function showCartToast(message){const text=String(message||'').trim();if(!text)return;const isError=/nije uspjelo|rasprodan|odaberite|nije dostup|greška|na stanju|smanjena na dostupno/i.test(text);if(!isError)return;let toast=document.querySelector('.cart-toast');if(!toast){toast=document.createElement('p');toast.className='cart-toast';document.body.appendChild(toast);}
toast.textContent=text;toast.classList.add('cart-toast--visible');clearTimeout(showCartToast.timer);showCartToast.timer=setTimeout(()=>{toast.classList.remove('cart-toast--visible');},2200);}
window.showCartToast=showCartToast;async function addVariationToCart(slug,variationId){const body=new URLSearchParams({variation_id:variationId,quantity:'1',stay:'1',});const response=await fetch(`/artikal/${slug}/dodaj/`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:body.toString(),});const data=await response.json();if(!response.ok||!data.ok){throw new Error(data.message||'Dodavanje u korpu nije uspjelo.');}
updateCartBadge(data.cart_count);showCartToast(data.message);if(data.upsell_html){handleUpsellResponse(data.upsell_html);}
return data;}
document.querySelectorAll('[data-copy-target]').forEach((button)=>{button.addEventListener('click',async()=>{const target=document.getElementById(button.dataset.copyTarget);if(!target)return;const text=target.textContent.trim();const fillId=button.dataset.fillInput;if(fillId){const input=document.getElementById(fillId);if(input){input.value=text;input.dispatchEvent(new Event('input',{bubbles:true}));input.focus();}}
try{await navigator.clipboard.writeText(text);const original=button.textContent;button.textContent=fillId?'Uneseno!':'Kopirano!';setTimeout(()=>{button.textContent=original;},1500);}catch{if(fillId){const original=button.textContent;button.textContent='Uneseno!';setTimeout(()=>{button.textContent=original;},1500);}else{button.textContent='Greška';}}});});function buildPriceHtml(price,originalPrice,onSale){const sale=Number(price);const formatted=Number.isFinite(sale)?sale.toFixed(2):String(price);if(onSale===true||onSale==='true'){const base=Number(originalPrice);const original=Number.isFinite(base)?base.toFixed(2):String(originalPrice);return(`<div class="product-sale-box">`+`<span class="card-dwell-label">AKCIJA</span>`+`<span class="price-sale card-dwell-now">${formatted}<span class="price-currency"> KM</span></span>`+`</div>`+`<span class="price-original card-dwell-was">${original}<span class="price-currency"> KM</span></span>`);}
return`<span class="price-current">${formatted}<span class="price-currency"> KM</span></span>`;}
function updateLoyaltyPoints(card,price){if(!card)return;const wrap=card.querySelector('[data-loyalty-points]');if(!wrap)return;const n=Number(price);if(!Number.isFinite(n)||n<=0){wrap.hidden=true;return;}
const bodovi=Math.round(n);wrap.hidden=false;wrap.dataset.loyaltyPrice=String(n);const valueEl=wrap.querySelector('.loyalty-points__value');if(valueEl)valueEl.textContent=`+${bodovi} bodova`;const tipStrong=wrap.querySelector('.loyalty-points__tooltip p strong');if(tipStrong)tipStrong.textContent=`+${bodovi} bodova`;}
const SiteModalQueue=(function createSiteModalQueue(){const queue=[];let activeId=null;let drainTimer=null;function anyVisible(){return!!document.querySelector('.site-popup-overlay.is-visible');}
function isBusy(){if(activeId)return true;if(anyVisible())return true;if(document.body.classList.contains('popup-open'))return true;return false;}
function findIndex(id){if(!id)return-1;return queue.findIndex((job)=>job&&job.id===id);}
function activate(job){if(!job||typeof job.show!=='function')return false;if(typeof job.canShow==='function'&&!job.canShow())return false;activeId=job.id||'active';try{job.show();}catch(err){activeId=null;return false;}
return true;}
function processNext(){if(anyVisible()||document.body.classList.contains('popup-open')){if(!activeId)activeId='external-visible';return;}
activeId=null;while(queue.length){const job=queue.shift();if(activate(job))return;}}
function enqueue(job){if(!job||typeof job.show!=='function')return;const id=job.id||'';if(id&&activeId===id)return;if(id&&findIndex(id)!==-1)return;if(isBusy()||queue.length){queue.push(job);if(!activeId&&!anyVisible()&&!document.body.classList.contains('popup-open')){processNext();}
return;}
if(!activate(job)){processNext();}}
function notifyClosed(closedId){if(closedId&&activeId&&closedId!==activeId){}else{activeId=null;}
if(drainTimer)clearTimeout(drainTimer);drainTimer=window.setTimeout(()=>{drainTimer=null;if(anyVisible()){if(!activeId)activeId='external-visible';return;}
document.body.classList.remove('popup-open');activeId=null;processNext();},80);}
function getActiveId(){return activeId;}
function peekQueueLength(){return queue.length;}
return{enqueue,notifyClosed,anyVisible,isBusy,processNext,getActiveId,peekQueueLength,};})();window.SiteModalQueue=SiteModalQueue;const sitePopupOverlays=Array.from(document.querySelectorAll('.site-popup-overlay[data-popup-id]'),);if(sitePopupOverlays.length){const pageStartedAt=Date.now();const presentedThisVisit=new Set();let activeOverlay=null;let activeCountdownInterval=null;function formatCountdown(ms){const totalSec=Math.max(0,Math.floor(ms/1000));const h=Math.floor(totalSec/3600);const m=Math.floor((totalSec%3600)/60);const s=totalSec%60;return`${String(h).padStart(2, '0')}:${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;}
function popupCooldownMs(overlay){const days=parseInt(overlay.dataset.popupCooldownDays||'0',10);if(!Number.isFinite(days)||days<=0){return 0;}
return days*24*60*60*1000;}
function popupStorageKeys(overlay){const popupId=overlay.dataset.popupId||'default';return{lastShownKey:`site_popup_last_shown_${popupId}`,sessionShownKey:`site_popup_shown_session_${popupId}`,};}
function stopCountdown(){if(activeCountdownInterval){clearInterval(activeCountdownInterval);activeCountdownInterval=null;}}
function shouldShowPopup(overlay){const popupId=overlay.dataset.popupId||'default';if(presentedThisVisit.has(popupId)){return false;}
const{lastShownKey,sessionShownKey}=popupStorageKeys(overlay);if(sessionStorage.getItem(sessionShownKey)){return false;}
const cooldownMs=popupCooldownMs(overlay);if(cooldownMs>0){const last=parseInt(localStorage.getItem(lastShownKey)||'0',10);if(last&&(Date.now()-last<=cooldownMs)){return false;}}
return true;}
function markPopupSeen(overlay){const popupId=overlay.dataset.popupId||'default';presentedThisVisit.add(popupId);const{lastShownKey,sessionShownKey}=popupStorageKeys(overlay);sessionStorage.setItem(sessionShownKey,'1');if(popupCooldownMs(overlay)>0){localStorage.setItem(lastShownKey,Date.now().toString());}}
function popupJobId(overlay){return`akcija-${overlay.dataset.popupId || 'default'}`;}
function hidePopup(overlay){if(!overlay)return;overlay.classList.remove('is-visible');overlay.hidden=true;if(activeOverlay===overlay){activeOverlay=null;document.body.classList.remove('popup-open');}}
function closePopup(overlay,recordSeen=true){if(!overlay)return;const jobId=popupJobId(overlay);stopCountdown();hidePopup(overlay);if(recordSeen){markPopupSeen(overlay);}
SiteModalQueue.notifyClosed(jobId);}
function startCountdown(overlay){const countdownEl=overlay.querySelector('[data-popup-countdown]');const endValue=overlay.dataset.countdownEnd;if(!countdownEl||!endValue)return;const endMs=Date.parse(endValue);if(!Number.isFinite(endMs))return;const tick=()=>{const diff=endMs-Date.now();if(diff<=0){countdownEl.textContent='00:00:00';stopCountdown();return;}
countdownEl.textContent=formatCountdown(diff);};tick();stopCountdown();activeCountdownInterval=setInterval(tick,1000);}
function showOverlayNow(overlay){const jobId=popupJobId(overlay);if(!overlay||!shouldShowPopup(overlay)){SiteModalQueue.notifyClosed(jobId);return;}
const visibleOther=document.querySelector('.site-popup-overlay.is-visible');if((visibleOther&&visibleOther!==overlay)||(activeOverlay&&activeOverlay!==overlay&&activeOverlay.classList.contains('is-visible'))){SiteModalQueue.notifyClosed(jobId);SiteModalQueue.enqueue({id:jobId,canShow:()=>shouldShowPopup(overlay),show:()=>showOverlayNow(overlay),});return;}
const popupImage=overlay.querySelector('.site-popup-image');if(popupImage&&popupImage.dataset.src&&!popupImage.getAttribute('src')){popupImage.setAttribute('src',popupImage.dataset.src);}
overlay.hidden=false;overlay.classList.add('is-visible');document.body.classList.add('popup-open');activeOverlay=overlay;startCountdown(overlay);}
function openPopup(overlay){if(!overlay||!shouldShowPopup(overlay)){return;}
if(activeOverlay===overlay&&overlay.classList.contains('is-visible')){return;}
const jobId=popupJobId(overlay);SiteModalQueue.enqueue({id:jobId,canShow:()=>shouldShowPopup(overlay),show:()=>showOverlayNow(overlay),});}
const popupQueue=sitePopupOverlays.filter((overlay)=>shouldShowPopup(overlay)).sort((a,b)=>{const delayA=parseInt(a.dataset.popupDelay||'0',10);const delayB=parseInt(b.dataset.popupDelay||'0',10);if(delayA!==delayB){return delayA-delayB;}
return parseInt(a.dataset.popupId||'0',10)-parseInt(b.dataset.popupId||'0',10);});const onProductPage=/^\/artikal\/[^/]+\/?$/.test(window.location.pathname||'');popupQueue.forEach((overlay,index)=>{let delaySec=parseInt(overlay.dataset.popupDelay||'0',10);const tip=overlay.dataset.akcijaTip||'';if(onProductPage&&tip==='bundle'){delaySec=2;}
const showAt=pageStartedAt+Math.max(0,delaySec)*1000+index*30;const waitMs=Math.max(0,showAt-Date.now());window.setTimeout(()=>{openPopup(overlay);},waitMs);});sitePopupOverlays.forEach((overlay)=>{overlay.querySelector('[data-popup-close]')?.addEventListener('click',()=>{closePopup(overlay,true);});const cta=overlay.querySelector('.site-popup-cta');if(cta&&cta.tagName==='A'){cta.addEventListener('click',()=>{markPopupSeen(overlay);},{once:true});}
const akcijaForms=overlay.querySelectorAll('.akcija-popup-add-form');akcijaForms.forEach((akcijaForm)=>{if(akcijaForm.dataset.popupSubmitBound==='1')return;akcijaForm.dataset.popupSubmitBound='1';akcijaForm.addEventListener('submit',async(e)=>{e.preventDefault();const submitBtn=akcijaForm.querySelector('button[type="submit"], .site-popup-cta');if(submitBtn)submitBtn.disabled=true;const tip=(overlay.dataset.akcijaTip||'');const isBundle=tip==='bundle';const isQtyDeal=tip==='qty_deal';try{const formData=new FormData(akcijaForm);formData.set('stay','1');if((tip==='timer'||tip==='gratis'||isBundle||isQtyDeal)&&overlay.dataset.akcijaId&&!formData.get('akcija_id')){formData.set('akcija_id',overlay.dataset.akcijaId);}
if(isBundle&&!formData.get('quantity')){formData.set('quantity','1');}
const response=await fetch(akcijaForm.action,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:new URLSearchParams(formData).toString(),});const data=await response.json();if(!response.ok||!data.ok){throw new Error(data.message||'Dodavanje u korpu nije uspjelo.');}
if(data.requires_bundle_confirm){const n=parseInt(data.available_sets,10)||0;const ok=window.confirm(data.message||'Nema dovoljno setova na stanju.');if(ok&&n>0){formData.set('bundle_confirm','1');formData.set('quantity',String(n));const retry=await fetch(akcijaForm.action,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:new URLSearchParams(formData).toString(),});const retryData=await retry.json();if(!retry.ok||!retryData.ok){throw new Error(retryData.message||'Dodavanje u korpu nije uspjelo.');}
updateCartBadge(retryData.cart_count||0);showCartToast(retryData.message||'Artikal je dodan u korpu.');if(retryData.meta_add_to_cart&&typeof window.trackMetaAddToCart==='function'){window.trackMetaAddToCart(retryData.meta_add_to_cart);}}
return;}
updateCartBadge(data.cart_count||0);showCartToast(data.message||'Artikal je dodan u korpu.');if(data.meta_add_to_cart&&typeof window.trackMetaAddToCart==='function'){window.trackMetaAddToCart(data.meta_add_to_cart);}
if(isBundle||isQtyDeal){closePopup(overlay,false);}else{closePopup(overlay,true);}}catch(err){showCartToast(err.message||'Dodavanje u korpu nije uspjelo.');}finally{if(submitBtn)submitBtn.disabled=false;}});});});}
const UPSELL_SESSION_KEY='upsell_popup_consumed_session';function isUpsellConsumed(){return sessionStorage.getItem(UPSELL_SESSION_KEY)==='1';}
function markUpsellConsumed(){sessionStorage.setItem(UPSELL_SESSION_KEY,'1');fetch('/upsell/odbaci/',{method:'POST',headers:{'X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},}).catch(()=>{});}
function formatUpsellCountdown(ms){const totalSec=Math.max(0,Math.floor(ms/1000));const h=Math.floor(totalSec/3600);const m=Math.floor((totalSec%3600)/60);const s=totalSec%60;return`${String(h).padStart(2, '0')}:${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;}
function initUpsellPopup(overlay){if(!overlay)return;if(isUpsellConsumed()){overlay.remove();return;}
let countdownInterval=null;function stopUpsellCountdown(){if(countdownInterval){clearInterval(countdownInterval);countdownInterval=null;}}
function startUpsellCountdown(){const countdownEl=overlay.querySelector('[data-upsell-countdown]');const minutes=parseInt(overlay.dataset.countdownMinutes||'15',10);if(!countdownEl||!Number.isFinite(minutes)||minutes<=0)return;const endMs=Date.now()+minutes*60*1000;const tick=()=>{const diff=endMs-Date.now();if(diff<=0){countdownEl.textContent='00:00:00';stopUpsellCountdown();return;}
countdownEl.textContent=formatUpsellCountdown(diff);};tick();stopUpsellCountdown();countdownInterval=setInterval(tick,1000);}
function upsellJobId(){return`upsell-${overlay.dataset.upsellId || 'default'}`;}
function closeUpsell(recordConsumed=true){stopUpsellCountdown();overlay.classList.remove('is-visible');overlay.hidden=true;document.body.classList.remove('popup-open');if(recordConsumed){markUpsellConsumed();}
if(window.SiteModalQueue){window.SiteModalQueue.notifyClosed(upsellJobId());}}
function showUpsellNow(){if(isUpsellConsumed()||!overlay.isConnected){overlay.remove();if(window.SiteModalQueue){window.SiteModalQueue.notifyClosed(upsellJobId());}
return;}
const other=document.querySelector('.site-popup-overlay.is-visible');if(other&&other!==overlay){if(window.SiteModalQueue){window.SiteModalQueue.notifyClosed(upsellJobId());window.SiteModalQueue.enqueue({id:upsellJobId(),canShow:()=>!isUpsellConsumed()&&overlay.isConnected,show:showUpsellNow,});}
return;}
overlay.hidden=false;overlay.classList.add('is-visible');document.body.classList.add('popup-open');startUpsellCountdown();}
function openUpsell(){if(isUpsellConsumed()){overlay.remove();return;}
if(window.SiteModalQueue){window.SiteModalQueue.enqueue({id:upsellJobId(),canShow:()=>!isUpsellConsumed()&&overlay.isConnected,show:showUpsellNow,});}else{showUpsellNow();}}
openUpsell();overlay.querySelector('#upsellPopupClose')?.addEventListener('click',()=>{closeUpsell(true);});overlay.querySelectorAll('.upsell-add-form').forEach((form)=>{form.addEventListener('submit',async(e)=>{e.preventDefault();const submitBtn=form.querySelector('button[type="submit"]');if(submitBtn)submitBtn.disabled=true;try{const formData=new FormData(form);formData.set('stay','1');const response=await fetch(form.action,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:new URLSearchParams(formData).toString(),});const data=await response.json();if(!response.ok||!data.ok){throw new Error(data.message||'Dodavanje u korpu nije uspjelo.');}
updateCartBadge(data.cart_count||0);showCartToast(data.message||'Artikal je dodan u korpu.');closeUpsell(true);}catch(err){showCartToast(err.message||'Dodavanje u korpu nije uspjelo.');}finally{if(submitBtn)submitBtn.disabled=false;}});});}
function handleUpsellResponse(html){if(!html||isUpsellConsumed())return;document.getElementById('upsellPopupOverlay')?.remove();document.body.insertAdjacentHTML('beforeend',html);const overlay=document.getElementById('upsellPopupOverlay');if(overlay){initUpsellPopup(overlay);}}
window.handleUpsellResponse=handleUpsellResponse;const upsellOverlay=document.getElementById('upsellPopupOverlay');if(upsellOverlay){initUpsellPopup(upsellOverlay);}
function bindCheckoutUpsellForms(root=document){root.querySelectorAll('.checkout-upsell-add-form').forEach((form)=>{if(form.dataset.checkoutUpsellBound==='1')return;form.dataset.checkoutUpsellBound='1';form.addEventListener('submit',async(e)=>{e.preventDefault();const submitBtn=form.querySelector('button[type="submit"]');if(submitBtn)submitBtn.disabled=true;try{const formData=new FormData(form);formData.set('stay','1');const response=await fetch(form.action,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:new URLSearchParams(formData).toString(),});const data=await response.json();if(!response.ok||!data.ok){throw new Error(data.message||'Dodavanje u korpu nije uspjelo.');}
updateCartBadge(data.cart_count||0);showCartToast(data.message||'Artikal je dodan u korpu.');if(data.checkout_items_html){const list=document.getElementById('checkoutItemsList');if(list)list.innerHTML=data.checkout_items_html;}
if(data.checkout_totals_html){const totals=document.getElementById('checkoutOrderTotals');if(totals)totals.innerHTML=data.checkout_totals_html;}
if(data.checkout_upsell_html!==undefined){const upsellSection=document.getElementById('checkoutUpsellSection');if(upsellSection){upsellSection.innerHTML=data.checkout_upsell_html;bindCheckoutUpsellForms(upsellSection);}}
const checkoutPage=document.querySelector('.checkout-page[data-meta-initiate-checkout]');if(checkoutPage&&data.cart_total){checkoutPage.dataset.value=data.cart_total;}}catch(err){showCartToast(err.message||'Dodavanje u korpu nije uspjelo.');}finally{if(submitBtn)submitBtn.disabled=false;}});});}
bindCheckoutUpsellForms();const ponudaOverlay=document.getElementById('gratisOfferOverlay');const ponudaImage=document.getElementById('gratisOfferImage');const ponudaTitle=document.getElementById('gratisOfferTitle');const ponudaText=document.getElementById('gratisOfferText');const ponudaHeadline=document.getElementById('gratisOfferHeadline');const ponudaLabel=document.getElementById('gratisOfferLabel');const ponudaBadge=document.getElementById('gratisOfferBadge');const ponudaPrices=document.getElementById('gratisOfferPrices');const ponudaOriginal=document.getElementById('gratisOfferOriginal');const ponudaDiscounted=document.getElementById('gratisOfferDiscounted');const ponudaAccept=document.getElementById('gratisOfferAccept');const ponudaDecline=document.getElementById('gratisOfferDecline');const ponudaClose=document.getElementById('gratisOfferClose');const ponudaQtyInput=document.getElementById('gratisOfferQty');const ponudaQtyMinus=document.getElementById('gratisOfferQtyMinus');const ponudaQtyPlus=document.getElementById('gratisOfferQtyPlus');let pendingPonuda=null;function getPonudaOfferQty(){if(!ponudaQtyInput)return 1;let n=parseInt(ponudaQtyInput.value,10);if(!Number.isFinite(n)||n<1)n=1;if(n>99)n=99;return n;}
function setPonudaOfferQty(value){if(!ponudaQtyInput)return;let n=parseInt(value,10);if(!Number.isFinite(n)||n<1)n=1;if(n>99)n=99;ponudaQtyInput.value=String(n);if(ponudaQtyMinus)ponudaQtyMinus.disabled=n<=1;if(ponudaQtyPlus)ponudaQtyPlus.disabled=n>=99;}
function closePonudaOfferModal(){if(!ponudaOverlay)return;ponudaOverlay.classList.remove('is-visible');ponudaOverlay.hidden=true;document.body.classList.remove('popup-open');pendingPonuda=null;}
function openPonudaOfferModal(offer,slug,variationId){if(!ponudaOverlay||!offer||!slug)return;if(document.getElementById('mainAddToCartForm')||document.getElementById('productDetailInfo')){return;}
pendingPonuda={offer,slug,variationId:variationId||''};if(ponudaLabel)ponudaLabel.textContent=offer.label||'Dobra kupovina';if(ponudaTitle)ponudaTitle.textContent=offer.gratis_naziv||'';if(ponudaHeadline){ponudaHeadline.textContent=offer.headline||'';ponudaHeadline.hidden=!offer.headline;}
if(ponudaText){if(offer.has_discount){const pctHtml=offer.is_full_discount?'<strong class="gratis-offer-pct">GRATIS</strong>':`<strong class="gratis-offer-pct">−${offer.pct}%</strong>`;ponudaText.innerHTML=`Želite li uz ovaj artikal i ovo sa <strong>AKCIJSKOM</strong> cijenom ${pctHtml}?`;}else{ponudaText.innerHTML='Želite li uz ovaj artikal i ovo?';}}
if(ponudaImage){if(offer.slika_url){ponudaImage.src=offer.slika_url;ponudaImage.alt=offer.gratis_naziv||'';ponudaImage.hidden=false;}else{ponudaImage.removeAttribute('src');ponudaImage.alt='';ponudaImage.hidden=true;}}
if(ponudaPrices&&ponudaOriginal&&ponudaDiscounted){if(offer.has_discount){ponudaOriginal.textContent=`${offer.original_price} KM`;ponudaOriginal.hidden=false;ponudaDiscounted.textContent=`${offer.discounted_price} KM`;}else{ponudaOriginal.textContent='';ponudaOriginal.hidden=true;ponudaDiscounted.textContent=`${offer.discounted_price || offer.original_price} KM`;}
ponudaPrices.hidden=false;}
if(ponudaBadge){if(offer.badge){ponudaBadge.textContent=offer.badge;ponudaBadge.hidden=false;}else{ponudaBadge.textContent='';ponudaBadge.hidden=true;}}
setPonudaOfferQty(1);if(ponudaAccept)ponudaAccept.textContent='DA — dodaj i ovo';if(ponudaDecline)ponudaDecline.textContent='NE — samo ovaj artikal';ponudaOverlay.hidden=false;requestAnimationFrame(()=>{ponudaOverlay.classList.add('is-visible');});document.body.classList.add('popup-open');}
const qtyDealOverlayMain=document.getElementById('qtyDealOfferOverlay');const qtyDealCloseMain=document.getElementById('qtyDealOfferClose');const qtyDealDeclineMain=document.getElementById('qtyDealOfferDecline');const qtyDealTitleMain=document.getElementById('qtyDealOfferTitle');const qtyDealKickerMain=document.getElementById('qtyDealOfferKicker');const qtyDealMessageMain=document.getElementById('qtyDealOfferMessage');const qtyDealSaveMain=document.getElementById('qtyDealOfferSave');const qtyDealBaseMain=document.getElementById('qtyDealOfferBase');const qtyDealImageMain=document.getElementById('qtyDealOfferImage');const qtyDealTiersMain=document.getElementById('qtyDealOfferTiers');let pendingQtyDeal=null;function closeQtyDealOfferModal(){if(!qtyDealOverlayMain)return;qtyDealOverlayMain.classList.remove('is-visible');qtyDealOverlayMain.hidden=true;document.body.classList.remove('popup-open');pendingQtyDeal=null;if(qtyDealTiersMain)qtyDealTiersMain.innerHTML='';}
function openQtyDealOfferModal(offer,slug,variationId,extraFields){if(!qtyDealOverlayMain||!offer||!slug)return;if(document.getElementById('mainAddToCartForm')||document.getElementById('productDetailInfo')){return;}
pendingQtyDeal={offer,slug,variationId:variationId||'',extraFields:extraFields||{},};if(qtyDealKickerMain){qtyDealKickerMain.textContent=offer.headline||'Kupi više — veći popust';}
if(qtyDealTitleMain)qtyDealTitleMain.textContent=offer.product_name||'';if(qtyDealMessageMain){qtyDealMessageMain.textContent=offer.message||'Uzmi veću količinu i uštedi. Ili nastavi s onim što si već izabrao.';}
if(qtyDealBaseMain){qtyDealBaseMain.textContent=offer.base_price?`Regularna cijena: ${offer.base_price} KM / kom`:'';}
if(qtyDealSaveMain){if(offer.best_usteda){qtyDealSaveMain.hidden=false;qtyDealSaveMain.textContent=`Uštedi do ${offer.best_usteda} KM`;}else{qtyDealSaveMain.hidden=true;}}
if(qtyDealImageMain){if(offer.image_url){qtyDealImageMain.src=offer.image_url;qtyDealImageMain.alt=offer.product_name||'';qtyDealImageMain.hidden=false;}else{qtyDealImageMain.removeAttribute('src');qtyDealImageMain.hidden=true;}}
if(qtyDealDeclineMain){qtyDealDeclineMain.textContent=offer.decline_label||'Ne, hvala — dodaj samo moju količinu';}
if(qtyDealTiersMain){const options=offer.options||[];qtyDealTiersMain.innerHTML=options.map((opt)=>{const bestClass=opt.is_best?' is-best':'';const bestTag=opt.is_best?'<span class="qty-deal-offer-tier-best-tag">Najbolja ušteda</span>':'';return(`<button type="button" class="qty-deal-offer-tier${bestClass}" `+`data-tier-id="${opt.id}" data-quantity="${opt.quantity}">`+
bestTag+`<span class="qty-deal-offer-tier-top">`+`<span class="qty-deal-offer-tier-qty">×${opt.quantity}</span>`+`<span class="qty-deal-offer-tier-badge">-${opt.pct_label}%</span>`+`</span>`+`<span class="qty-deal-offer-tier-price">`+`<span class="qty-deal-offer-tier-was">${opt.line_bazna} KM</span>`+`<span class="qty-deal-offer-tier-now">${opt.line_snizena} KM</span>`+`</span>`+`<span class="qty-deal-offer-tier-meta">`+`Ušteda ${opt.usteda} KM · ${opt.unit_snizena} KM / kom`+`</span></button>`);}).join('');qtyDealTiersMain.querySelectorAll('.qty-deal-offer-tier').forEach((btn)=>{btn.addEventListener('click',()=>{const tierId=btn.getAttribute('data-tier-id')||'';const qty=btn.getAttribute('data-quantity')||'2';submitQtyDealTier(tierId,qty);});});}
qtyDealOverlayMain.hidden=false;requestAnimationFrame(()=>{qtyDealOverlayMain.classList.add('is-visible');});document.body.classList.add('popup-open');}
async function submitQtyDealTier(tierId,quantity){if(!pendingQtyDeal)return;const{offer,slug,variationId}=pendingQtyDeal;closeQtyDealOfferModal();await catalogAddProductToCart(slug,variationId,{akcija_id:String(offer.akcija_id),tier_id:String(tierId),quantity:String(quantity),});}
async function submitQtyDealDecline(){if(!pendingQtyDeal)return;const{slug,variationId,extraFields}=pendingQtyDeal;closeQtyDealOfferModal();await catalogAddProductToCart(slug,variationId,{...(extraFields||{}),qty_deal_choice:'no',});}
const onProductDetailPage=!!(document.getElementById('mainAddToCartForm')||document.getElementById('productDetailInfo'));if(!onProductDetailPage){if(qtyDealDeclineMain&&!qtyDealDeclineMain.dataset.qtyDealBound){qtyDealDeclineMain.dataset.qtyDealBound='1';qtyDealDeclineMain.addEventListener('click',()=>submitQtyDealDecline());}
if(qtyDealCloseMain&&!qtyDealCloseMain.dataset.qtyDealBound){qtyDealCloseMain.dataset.qtyDealBound='1';qtyDealCloseMain.addEventListener('click',()=>submitQtyDealDecline());}}
async function submitPonudaChoice(choice){if(!pendingPonuda)return;const{offer,slug,variationId}=pendingPonuda;const offerQty=getPonudaOfferQty();closePonudaOfferModal();await catalogAddProductToCart(slug,variationId,{gratis_choice:choice,gratis_akcija_id:String(offer.akcija_id),gratis_quantity:String(choice==='yes'?offerQty:1),});}
const isProductDetailPage=!!(document.getElementById('mainAddToCartForm')||document.getElementById('productDetailInfo'));if(!isProductDetailPage){if(ponudaAccept&&!ponudaAccept.dataset.ponudaBound){ponudaAccept.dataset.ponudaBound='1';ponudaAccept.addEventListener('click',()=>submitPonudaChoice('yes'));}
if(ponudaDecline&&!ponudaDecline.dataset.ponudaBound){ponudaDecline.dataset.ponudaBound='1';ponudaDecline.addEventListener('click',()=>submitPonudaChoice('no'));}
if(ponudaClose&&!ponudaClose.dataset.ponudaBound){ponudaClose.dataset.ponudaBound='1';ponudaClose.addEventListener('click',()=>submitPonudaChoice('no'));}
if(ponudaQtyMinus&&!ponudaQtyMinus.dataset.ponudaBound){ponudaQtyMinus.dataset.ponudaBound='1';ponudaQtyMinus.addEventListener('click',(e)=>{e.preventDefault();e.stopPropagation();setPonudaOfferQty(getPonudaOfferQty()-1);});}
if(ponudaQtyPlus&&!ponudaQtyPlus.dataset.ponudaBound){ponudaQtyPlus.dataset.ponudaBound='1';ponudaQtyPlus.addEventListener('click',(e)=>{e.preventDefault();e.stopPropagation();setPonudaOfferQty(getPonudaOfferQty()+1);});}
if(ponudaQtyInput&&!ponudaQtyInput.dataset.ponudaBound){ponudaQtyInput.dataset.ponudaBound='1';const sync=()=>setPonudaOfferQty(getPonudaOfferQty());ponudaQtyInput.addEventListener('change',sync);ponudaQtyInput.addEventListener('input',sync);}}
async function catalogAddProductToCart(slug,variationId='',extraFields={}){const body=new URLSearchParams({quantity:'1',stay:'1',});if(variationId){body.set('variation_id',variationId);}
Object.entries(extraFields||{}).forEach(([key,value])=>{if(value!=null&&value!=='')body.set(key,value);});const response=await fetch(`/artikal/${slug}/dodaj/`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':getCsrfToken(),'X-Requested-With':'XMLHttpRequest',},body:body.toString(),});const data=await response.json();if(!response.ok||!data.ok){throw new Error(data.message||'Dodavanje u korpu nije uspjelo.');}
if(data.requires_qty_deal_choice&&data.qty_deal_offer){openQtyDealOfferModal(data.qty_deal_offer,slug,variationId,extraFields);return data;}
if(data.requires_gratis_choice&&data.gratis_offer){openPonudaOfferModal(data.gratis_offer,slug,variationId);return data;}
updateCartBadge(data.cart_count||0);showCartToast(data.message||'Artikal je dodan u korpu.');if(data.meta_add_to_cart&&typeof window.trackMetaAddToCart==='function'){window.trackMetaAddToCart(data.meta_add_to_cart);}
if(data.upsell_html){handleUpsellResponse(data.upsell_html);}
return data;}
const catalogVariationModal=document.getElementById('catalogVariationModal');const catalogVariationModalProduct=document.getElementById('catalogVariationModalProduct');const catalogVariationModalOptions=document.getElementById('catalogVariationModalOptions');let catalogVariationModalSlug='';function closeCatalogVariationModal(){if(!catalogVariationModal)return;catalogVariationModal.hidden=true;catalogVariationModal.setAttribute('aria-hidden','true');document.body.classList.remove('catalog-variation-modal-open');catalogVariationModalSlug='';if(catalogVariationModalOptions){catalogVariationModalOptions.innerHTML='';}}
function openCatalogVariationModal(card,triggerBtn){if(!catalogVariationModal||!catalogVariationModalOptions)return;const slug=triggerBtn.dataset.productSlug||card?.dataset.productSlug||'';const productName=card?.dataset.defaultName||card?.querySelector('[data-product-name]')?.textContent?.trim()||'';const variations=Array.from(card?.querySelectorAll('[data-catalog-variation]')||[]);if(!slug||!variations.length)return;catalogVariationModalSlug=slug;if(catalogVariationModalProduct){catalogVariationModalProduct.textContent=productName;}
catalogVariationModalOptions.innerHTML=variations.map((node)=>{const variationId=node.dataset.variationId||'';const variationName=node.dataset.variationName||'Model';return(`<button type="button" class="catalog-variation-modal__option" data-variation-id="${variationId}">${variationName}</button>`);}).join('');catalogVariationModal.hidden=false;catalogVariationModal.setAttribute('aria-hidden','false');document.body.classList.add('catalog-variation-modal-open');}
if(catalogVariationModal){catalogVariationModal.querySelectorAll('button[data-catalog-variation-close], .catalog-variation-modal__close',).forEach((node)=>{node.addEventListener('click',closeCatalogVariationModal);});catalogVariationModalOptions?.addEventListener('click',async(event)=>{const option=event.target.closest('[data-variation-id]');if(!option||!catalogVariationModalSlug)return;option.disabled=true;try{await catalogAddProductToCart(catalogVariationModalSlug,option.dataset.variationId||'');closeCatalogVariationModal();}catch(err){showCartToast(err.message||'Dodavanje u korpu nije uspjelo.');}finally{option.disabled=false;}});}
function bindCatalogAddButtons(root=document){root.querySelectorAll('[data-catalog-add]').forEach((button)=>{if(button.dataset.catalogAddBound==='1')return;button.dataset.catalogAddBound='1';button.addEventListener('click',async(event)=>{event.preventDefault();event.stopPropagation();if(button.disabled)return;const card=button.closest('.product-card--catalog, .product-card--home-mockup, .product-card');const slug=button.dataset.productSlug||card?.dataset.productSlug||'';if(button.dataset.pickVariation==='1'){openCatalogVariationModal(card,button);return;}
button.disabled=true;try{await catalogAddProductToCart(slug,button.dataset.variationId||'');}catch(err){showCartToast(err.message||'Dodavanje u korpu nije uspjelo.');}finally{button.disabled=false;}});});}
bindCatalogAddButtons();document.querySelectorAll('[data-product-card]').forEach((card)=>{const mainImage=card.querySelector('[data-main-image]');const nameEl=card.querySelector('[data-product-name]');const priceEl=card.querySelector('[data-product-price]:not([hidden])');const swatches=card.querySelectorAll('.variation-swatch');const defaultName=card.dataset.defaultName;const cartOnSwatch=card.hasAttribute('data-cart-on-swatch');swatches.forEach((swatch)=>{swatch.addEventListener('click',(e)=>{e.preventDefault();e.stopPropagation();if(card.querySelector('[data-dwell-card-deal], [data-dwell-card-flash].is-active')){return;}
swatches.forEach(s=>s.classList.remove('active'));swatch.classList.add('active');const name=swatch.dataset.name;const price=swatch.dataset.price;const originalPrice=swatch.dataset.originalPrice;const onSale=swatch.dataset.onSale;if(mainImage&&mainImage.tagName==='IMG'&&swatch.dataset.image){mainImage.src=swatch.dataset.image;}
if(nameEl&&name!==defaultName){nameEl.textContent=name;}else if(nameEl){nameEl.textContent=defaultName;}
const livePriceEl=card.querySelector('[data-product-price]:not([hidden])');if(livePriceEl&&price){livePriceEl.innerHTML=buildPriceHtml(price,originalPrice,onSale);updateLoyaltyPoints(card,price);}
if(cartOnSwatch&&swatch.dataset.productSlug&&swatch.dataset.variationId){addVariationToCart(swatch.dataset.productSlug,swatch.dataset.variationId).catch((err)=>{showCartToast(err.message);});}});});});(function initLoyaltyPointsTooltips(){const closeAll=(except)=>{document.querySelectorAll('[data-loyalty-points-info][aria-expanded="true"]').forEach((btn)=>{if(except&&btn===except)return;btn.setAttribute('aria-expanded','false');const tip=btn.closest('[data-loyalty-points]')?.querySelector('[data-loyalty-points-tooltip]');if(tip)tip.hidden=true;});};document.addEventListener('click',(e)=>{const btn=e.target.closest('[data-loyalty-points-info]');if(btn){e.preventDefault();e.stopPropagation();const wrap=btn.closest('[data-loyalty-points]');const tip=wrap&&wrap.querySelector('[data-loyalty-points-tooltip]');if(!tip)return;const open=btn.getAttribute('aria-expanded')==='true';closeAll(btn);btn.setAttribute('aria-expanded',open?'false':'true');tip.hidden=open;return;}
if(!e.target.closest('[data-loyalty-points]')){closeAll();}});document.addEventListener('keydown',(e)=>{if(e.key==='Escape')closeAll();});})();(function initCardDwellFlashTimers(){const blocks=document.querySelectorAll('[data-dwell-card-flash]');if(!blocks.length)return;function fmt(sec){sec=Math.max(0,Math.floor(sec));const m=Math.floor(sec/60);const s=sec%60;return m+':'+String(s).padStart(2,'0');}
function expireBlock(block){block.classList.remove('is-active');block.hidden=true;const parent=block.parentElement||block.closest('.product-card, .product-info, .product-card__price-block');const regular=parent?parent.querySelector('[data-dwell-regular-price]'):block.parentElement&&block.parentElement.querySelector('[data-dwell-regular-price]');if(regular){regular.hidden=false;regular.removeAttribute('hidden');}}
blocks.forEach((block)=>{const expiresTs=parseFloat(block.getAttribute('data-dwell-expires')||'0');const countdownEl=block.querySelector('[data-dwell-countdown]');if(!expiresTs||!countdownEl)return;const tick=()=>{const remaining=Math.floor(expiresTs-(Date.now()/1000));if(remaining<=0){expireBlock(block);return false;}
countdownEl.textContent=fmt(remaining);return true;};if(!tick())return;const timer=setInterval(()=>{if(!tick())clearInterval(timer);},1000);});})();});